---
navigationLabel: Home
---

# Home

This is `src/pages/index.mdx`

Code snippets can themed, see `gatsby-plugin-theme-ui/index.js`

```javascript
<SourceList>
  {source => (
    <ul>
      {source.map((edge, index) => {
        const {
          frontmatter: { title }
        } = edge.node;
        return <li key={index}>{title}</li>;
      })}
    </ul>
  )}
</SourceList>
```
